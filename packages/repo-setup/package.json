{
  "name": "@harness/repo-setup",
  "version": "1.0.0",
  "description": "Automated repository setup that creates a fully configured dev environment for every harness-built startup",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.4"
  }
}
