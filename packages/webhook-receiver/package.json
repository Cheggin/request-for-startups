{
  "name": "@startup-harness/webhook-receiver",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:channel": "bun run channel/index.ts",
    "dev:receiver": "bun run receiver/index.ts",
    "start": "bun run receiver/index.ts",
    "deploy:receiver": "railway up",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "convex": "^1.21.0"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
