{
  "name": "startup-harness",
  "version": "1.0.0",
  "description": "Autonomous startup builder plugin — 121 skills, 32 agents, deterministic skill-chain enforcement, 11-event-type hook coverage, bundled OMC runtime. Standalone: no dependency on oh-my-claudecode plugin.",
  "repository": "https://github.com/Cheggin/request-for-startups",
  "license": "MIT",
  "private": true,
  "bin": {
    "omc": "bridge/cli.cjs",
    "startup-harness": "bridge/cli.cjs"
  },
  "scripts": {
    "test": "bun run test:all",
    "test:all": "for dir in packages/*/; do (cd $dir && bun test 2>/dev/null) || true; done",
    "test:hooks": "node --test hooks/*.test.mjs .claude/hooks/*.test.mjs",
    "validate": "node scripts/validate-plugin.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "better-sqlite3": "^12.9.0"
  }
}
