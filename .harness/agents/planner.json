{
  "name": "planner",
  "description": "Strategic planning consultant with interview workflow (Opus)",
  "category": "orchestration",
  "mcpServers": {},
  "allowedTools": ["Read", "Bash", "Glob", "Grep", "Write", "Edit"],
  "fileScope": {
    "writable": [
      ".harness/plans/**",
      ".harness/knowledge/**",
      ".harness/handoffs/**"
    ],
    "readonly": [
      "agents/**",
      "skills/**",
      "packages/**",
      ".harness/**",
      ".claude/**",
      "src/**",
      "app/**",
      "convex/**"
    ],
    "blocked": [
      ".harness/agents/**",
      ".harness/agent-categories.yml"
    ]
  },
  "rules": [
    "Output is plans and handoff docs under .harness/plans/ — never source code",
    "Gather requirements via interview before proposing a plan",
    "Plans are consumed by builder agents — must be unambiguous and testable",
    "Cite the spec / issue / prior plan the new plan extends"
  ]
}
