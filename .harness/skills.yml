# Skill Configuration
# Controls which skills and hooks are active and defines custom skill chains.
# Read by .harness/hooks/setup.mjs and .harness/hooks/skill-config.mjs.

disabled: []

hooks:
  disabled: []

chains: {}
